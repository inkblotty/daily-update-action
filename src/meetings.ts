export const getMeetingIssues = async (repo) => {
    const label = "meeting"; // ensure that "Deep-dive" label is filtered out

    // get meetings in groups:
    // newly created meetings
    // meetings with a timeline beginning tomorrow
}
